$$
a_1x_1 + a_2x_2 + ... + a_n x_n = b
$$
An [[Equation]] where in the above form where the the a's are **coefficients**, x's are **variables**, n is the **dimension** (number of variables), e.g. $2x_1 + 4x_2 = 4$ has two dimensions